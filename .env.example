# Overture Maps CORS Proxy URL
# Local dev (run `cd worker && npm run dev` first):
VITE_OVERTURE_PROXY_URL=http://localhost:8787

# Production (after deploying the Cloudflare Worker):
# VITE_OVERTURE_PROXY_URL=https://draw2data-proxy.<your-subdomain>.workers.dev
